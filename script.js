//your JS code here. If required.
// Step 1: Access the current URL using window.location.href
const currentURL = window.location.href;

// Step 2: Count the number of characters in the URL using the length property
const urlLength = currentURL.length;

// Step 3: Print the length of the URL using alert()
alert("The length of the URL is: " + urlLength);
